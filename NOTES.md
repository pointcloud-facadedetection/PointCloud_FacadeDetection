# Teaching Notes

- 用户明确只学习 Linux 基础操作，当前不学习 Open3D，也不处理点云项目迁移。
- 教学语言：中文；偏好：短小、实践优先、每一步都有可观察结果。
- 环境：Ubuntu 24.04.1 LTS、WSL 2、WSLg，默认用户为 `ubuntu`。
- 课程顺序：路径导航 → 文件操作 → 查看与搜索文本 → 权限与 `sudo` → 软件包 → 进程和系统状态。
- 练习统一放在 `~/linux-practice`，避免误操作真实文件。
